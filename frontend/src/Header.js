import React, { Component } from 'react';
import './Header.css';

class Header extends Component {
    render() {
        return(
            <p className="headerpara">This is the header</p>
        )
    }
}

export default Header;